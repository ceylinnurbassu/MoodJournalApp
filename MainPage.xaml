<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoodJournalApp.MainPage"
             Title="Mood Journal">

    <!-- Gestalt principles consistent grouping (Frame + VerticalStackLayout) helps users perceive structure -->
   
    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="20" BackgroundColor="#CBF4FFDE">

            <!-- Aesthetic usability effect visually calm color palette -->
            <Frame BackgroundColor="#34F4FF2D" Padding="15" CornerRadius="12">
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label Text="Mood Journal"
                           FontSize="28"
                           FontAttributes="Bold"
                           TextColor="#FF0E4306" />
                
                </VerticalStackLayout>
            </Frame>

            <Label Text="How are you feeling today?"
                   FontSize="18"
                   HorizontalOptions="Center"
                   TextColor="{StaticResource Gray900}" />
            <Label x:Name="MoodLabel"
                   Text="Select your mood:"
                   FontSize="17"
                   HorizontalOptions="Center"
                   TextColor="{StaticResource Gray500}" />

           
            <!-- Fitts’s law large buttons for fast interaction -->
            <!-- Hick’s law limited to 5 differentiated choices -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                <Button Text="😊" FontSize="32" Clicked="OnMoodSelected" CommandParameter="Happy" />
                <Button Text="😐" FontSize="32" Clicked="OnMoodSelected" CommandParameter="Neutral" />
                <Button Text="😔" FontSize="32" Clicked="OnMoodSelected" CommandParameter="Sad" />
                <Button Text="😡" FontSize="32" Clicked="OnMoodSelected" CommandParameter="Angry" />
                <Button Text="😴" FontSize="32" Clicked="OnMoodSelected" CommandParameter="Tired" />
            </HorizontalStackLayout>

            
            <!-- Miller’s law input kept short to avoid overload -->
           <Editor x:Name="NoteEditor"
                    Placeholder="Write your thoughts..."
                    AutoSize="TextChanges"
                    HeightRequest="120"
                    BackgroundColor="{StaticResource NoteEnterenceColour}"
                    TextColor="{StaticResource Gray900}"
                    PlaceholderColor="{StaticResource Gray500}" />

            
            <!-- Doherty threshold immediate visual feedback -->
            <Button Text="Save Entry"
                    Clicked="OnSaveClicked"
                    WidthRequest="200"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource PrimaryDark}"
                    FontAttributes="Bold"
                    HorizontalOptions="Center" />

           
            <!-- ICommand Binding -->
            <!-- XAML Command Binding to ClearAllCommand -->
            <Button Text="Clear All"
                    Command="{Binding ClearAllCommand}"
                    WidthRequest="200"
                    BackgroundColor="{StaticResource Gray300}"
                    TextColor="{StaticResource Gray900}"
                    FontAttributes="Bold"
                    HorizontalOptions="Center" />

             <!-- Von restorff effect feedback label uses different color to stand out -->
            <Label x:Name="SavedNoteLabel"  
                   Text=""
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextColor="{StaticResource PrimaryDark}" />

            <Label Text="Last 7 Mood Entries"
                   FontAttributes="Bold"
                   FontSize="16"
                   TextColor="{StaticResource Gray900}" />

          <!-- Dynamic list display using ObservableCollection --> 
            <CollectionView x:Name="MoodList"
                HeightRequest="350"
                BackgroundColor="Transparent">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                   <!-- Gestalt grouped frame for each entry -->
                    <Frame Margin="5"
                        Padding="10"
                        BackgroundColor="{Binding MoodColor}"  
                        BorderColor="{StaticResource Gray300}"
                        CornerRadius="10"
                        HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                             <!-- Data Binding -->
                            <Label Grid.Row="0" Grid.Column="0"
                                Text="{Binding DisplayText}"
                                FontSize="14"
                                TextColor="{StaticResource Gray900}"
                                LineBreakMode="WordWrap" />
                            <Button Grid.Row="0" Grid.Column="1"
                                    Text="🗑️"
                                    Clicked="OnDeleteClicked"
                                    CommandParameter="{Binding}"
                                    BackgroundColor="Transparent"
                                    FontSize="14"
                                    TextColor="{StaticResource Gray500}" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>


        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
